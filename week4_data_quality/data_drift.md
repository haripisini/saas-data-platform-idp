Data Drift Detection

Data drift occurs when the structure or distribution of incoming data changes.

Examples:

- New unexpected columns in incoming files
- Missing required fields
- Sudden increase in null values

Basic checks can be implemented in the ingestion pipeline to detect schema drift and log alerts.